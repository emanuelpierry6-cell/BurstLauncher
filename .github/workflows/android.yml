name: Build Burst Launcher

on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      ANDROID_SDK_ROOT: /usr/local/lib/android/sdk

    steps:
    - name: Checkout do código
      uses: actions/checkout@v4

    - name: Configurar Java 17
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'

    - name: Configurar Gradle Oficial (7.5)
      uses: gradle/actions/setup-gradle@v3
      with:
        gradle-version: '7.5'

    - name: Limpar Conflitos de Repositórios com Segurança
      run: |
        python3 -c "
        import glob, re
        for path in glob.glob('**/settings.gradle*', recursive=True):
            with open(path, 'r', encoding='utf-8', errors='ignore') as f:
                content = f.read()
            # Remove completamente o bloco dependencyResolutionManagement para evitar conflitos
            content = re.sub(r'dependencyResolutionManagement\s*\{[^}]*\}', '', content, flags=re.DOTALL)
            with open(path, 'w', encoding='utf-8') as f:
                f.write(content)
            print(f'Ficheiro limpo com sucesso: {path}')
        "

    - name: Compilar APK
      run: gradle assembleDebug --stacktrace

    - name: Enviar APK gerado
      uses: actions/upload-artifact@v4
      with:
        name: Burst-Launcher-APK
        path: app/build/outputs/apk/debug/app-debug.apk
